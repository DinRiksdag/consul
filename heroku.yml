build:
  languages:
    - ruby
  packages:
    - imagemagick
run:
  web: bundle exec rails server -e ${RACK_ENV:-production}
  worker:  bundle exec rake db:migrate
  worker:  bundle exec rake db:dev_seed
